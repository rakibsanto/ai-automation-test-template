ollama>=0.3.0
playwright>=1.44.0
pytest>=8.2.0
pytest-playwright>=0.5.0
pytest-json-report>=1.5.0
pytest-html>=4.1.0
pytest-timeout>=2.3.1
Jinja2>=3.1.4
# Pillow — screenshot annotation (red banner with error message on failure shots)
Pillow>=10.0.0

# LangGraph — graph-based multi-agent orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# browser-use — autonomous browser AI agent (local Ollama, no API key)
# Enables: selector discovery, exploratory testing, flow validation
# Activate: BROWSER_USE_ENABLED=true BROWSER_USE_MODEL=qwen2.5:7b
browser-use>=0.1.0
langchain-ollama>=0.2.0
python-dotenv>=1.0.1
